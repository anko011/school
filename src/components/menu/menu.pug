include /src/components/button/button.pug

mixin menu(variant="primary", ...list)
    - const variantClass = `menu menu--${variant}`
    nav(class=variantClass)
        each item in list 
            +button(variant, item.text, item.icon, item.alt, item.isActive)